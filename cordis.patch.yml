# DSH WPS Plugin - Cordis bundle patch layer
# 金山文档 WPS 能力集成插件。
#
# 一个 bundle 的 patch 必须使用 `insert` 形式把插件行插入 profile 组合；
# `name` 是该包的包名（loader 通过 import(name) 解析），不是显示名。

- insert:
    - id: tool-wps
      name: dsh-wps-plugin
      config:
        enabled: true
        timeoutMs: 30000
